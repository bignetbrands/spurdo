// This file is intentionally a stub.
// The next.config.ts rewrite redirects "/" to "/landing.html" (the static file in /public).
// Visitors to spurdosparde.fun see the existing landing page; this React route is never rendered.

export default function Page() {
  return null;
}
